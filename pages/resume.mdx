import {ArticleLayout} from "../components/ArticleLayout";

# ブログ

* [HOME](/)
* ```javascript
  const a = 'a';
  ```

export default ({children}) =><ArticleLayout meta={{title: "ブログ"}}>{children}</ArticleLayout>;
